from __future__ import annotations

from typing import Any


def map_cicflowmeter_features(record: dict[str, Any]) -> dict[str, Any]:
    """Return a detached mapping pending empirical dataset compatibility checks."""
    return dict(record)

